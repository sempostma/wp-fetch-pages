{
  "name": "wp-fetch-pages",
  "version": "0.1.0",
  "description": "Fetches wordpress pages recursively.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sem Postma",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^6.0.0",
    "typescript": "^3.8.2"
  }
}
